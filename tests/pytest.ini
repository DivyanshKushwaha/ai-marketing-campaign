[pytest]
testpaths = .
addopts =
    --cov=src
    --cov-config=tests/pytest.ini
    --cov-fail-under=80
    --cov-report=
    -p no:cacheprovider
    -q

[coverage:run]
source = src
omit = src/main.py

[coverage:report]
fail_under = 80
